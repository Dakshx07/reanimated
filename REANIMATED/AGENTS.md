# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Agent Operating Rules
Follow the loop rules defined in `.agents/AGENT_LOOP.md` for every task in this repo.x